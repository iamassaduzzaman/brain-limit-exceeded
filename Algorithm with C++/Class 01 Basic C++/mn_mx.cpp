#include <iostream>
using namespace std;

int main(){

    int x = 10, y = 20;
    
    // min max build in function
    cout << "max = " << max(x, y) << '\n'; // return maximum between two number
    cout << "min = " << min(x, y) << '\n'; // return minimum between two number

    return 0;
}